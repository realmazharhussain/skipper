using Gtk 4.0;
using Adw 1;

template SkipperWindow: Adw.ApplicationWindow {  
  Box {
    orientation: vertical;
  
    .SkipperHeaderBar {}
    Box {
      orientation: horizontal;
    }
    .SkipperHeaderBar {}
  }
}
